<%@page import="result.ResultReplyDAO"%>
<%@ page language="java" contentType="text/html; charset=UTF-8"
    pageEncoding="UTF-8"%>
    
<%
	String cno = request.getParameter("cno");
	String pno = request.getParameter("pno");
	String no = request.getParameter("no");
	String mno = request.getParameter("mno");
	int replyMemNo = Integer.parseInt("mno");
	
	// memeber id 체크 -  현재 접속한 회원과 댓글작성자의 아이디와 일치하는지 여부 체크
	// session.getParameter 멤버아이디 추출
	// 댓글작성자와 일치하면 
	// if(replyMemNo === memNo ) {
	// 		댓글 삭제 실행
	// } else if(replyMemNo != memNo) {
	// 		alert(작성자만 삭제가능합니다);
	// }
	ResultReplyDAO dao = new ResultReplyDAO();
	System.out.println(cno);
	dao.deleteReplyByNo(Integer.parseInt(cno));
	
	response.sendRedirect("resultDetail.jsp?pno=" + pno + "&no=" + no );
%>    
